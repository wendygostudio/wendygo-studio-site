import fs from 'node:fs';import path from 'node:path';
const check=process.argv.includes('--check'),root=path.resolve('public'),labels={en:'English',es:'Español',de:'Deutsch',fr:'Français',it:'Italiano','pt-PT':'Português'},ui={en:['Language','Choose language'],es:['Idioma','Elegir idioma'],de:['Sprache','Sprache wählen'],fr:['Langue','Choisir la langue'],it:['Lingua','Scegli la lingua'],'pt-PT':['Idioma','Escolher idioma']};let changed=0,eligible=0;
function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(dir,e.name)):e.name.endsWith('.html')?[path.join(dir,e.name)]:[])}
function alternates(html){return Object.fromEntries([...html.matchAll(/<link\s+rel=["']alternate["']\s+hreflang=["']([^"']+)["']\s+href=["']([^"']+)["'][^>]*>/gi)].map(m=>[m[1],m[2]]))}
for(const file of walk(root)){let html=fs.readFileSync(file,'utf8');const routes=alternates(html),langs=Object.keys(labels);if(!langs.every(l=>routes[l]))continue;eligible++;const current=html.match(/<html\s+lang=["']([^"']+)/i)?.[1]||'en',[label,aria]=ui[current]||ui.en;const options=langs.map(l=>`<option value="${routes[l]}"${current===l?' selected':''}>${labels[l]}</option>`).join('');const widget=`<div class="wg-language-switcher"><label for="wg-language-select">${label}</label><select id="wg-language-select" aria-label="${aria}" onchange="if(this.value)location.href=this.value">${options}</select></div>`;const style=`<style id="wg-language-style">.wg-language-switcher{position:fixed;right:16px;bottom:16px;z-index:9999;display:flex;align-items:center;gap:8px;padding:8px 10px;border:1px solid #3a414e;border-radius:10px;background:rgba(19,21,26,.94);box-shadow:0 8px 28px rgba(0,0,0,.32);font:600 12px/1.2 system-ui,sans-serif;color:#9ca4b4}.wg-language-switcher select{max-width:130px;padding:6px 28px 6px 8px;border:1px solid #3a414e;border-radius:7px;background:#232831;color:#f0f1f4;font:600 12px system-ui,sans-serif}.wg-language-switcher select:focus-visible{outline:2px solid #e8a33d;outline-offset:2px}@media(max-width:520px){.wg-language-switcher{right:10px;bottom:10px}.wg-language-switcher label{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}}</style>`;html=html.replace(/<style id="wg-language-style">[\s\S]*?<\/style>/,'').replace(/<div class="wg-language-switcher">[\s\S]*?<\/div>/,'');html=html.replace('</head>',style+'</head>').replace('</body>',widget+'</body>');const old=fs.readFileSync(file,'utf8');if(old!==html){changed++;if(!check)fs.writeFileSync(file,html)}}
console.log(`${check?'Checked':'Ensured'} language switchers on ${eligible} pages; ${changed} ${check?'out of sync':'updated'}`);if(check&&changed)process.exit(1);
