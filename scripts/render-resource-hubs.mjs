import fs from 'node:fs';
import path from 'node:path';

const check = process.argv.includes('--check');
const products = JSON.parse(fs.readFileSync('data/products.json', 'utf8'));
const hubs = {
  'text-tools': {product:'textforge', en:['Text tools','Clean, extract and transform text without sending it to a server.'], es:['Herramientas de texto','Limpia, extrae y transforma texto sin enviarlo a un servidor.']},
  'image-tools': {product:'frameforge', en:['Image tools','Prepare images for social media, thumbnails and everyday publishing.'], es:['Herramientas de imagen','Prepara imágenes para redes sociales, miniaturas y publicación diaria.']},
  'file-conversion': {product:'convertforge', useCase:{en:['/use-cases/local-file-converter-chrome-extension/','Local file converter for Chrome'],es:['/es/use-cases/convertidor-archivos-extension-chrome/','Convertidor de archivos local para Chrome']}, en:['File conversion','Convert common image, document, audio and data formats locally.'], es:['Conversión de archivos','Convierte formatos habituales de imagen, documentos, audio y datos localmente.']},
  'infrastructure-security': {product:'scrubforge', en:['Infrastructure security','Share configurations and logs with less risk of exposing secrets.'], es:['Seguridad de infraestructura','Comparte configuraciones y registros con menos riesgo de exponer secretos.']},
  'eu-consumer-rights': {product:'claimforge', en:['EU consumer rights','Practical, source-aware guidance for warranties, returns, repairs and disputes.'], es:['Derechos del consumidor en la UE','Orientación práctica y basada en fuentes para garantías, devoluciones, reparaciones y disputas.']},
  'focus-productivity': {product:'slimeforge', en:['Focus and productivity','Build a sustainable focus routine with timers, goals and a companion that grows with you.'], es:['Concentración y productividad','Crea una rutina sostenible con temporizadores, objetivos y una mascota que crece contigo.']}
};
const esc = s => String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const articles = locale => {
  const root = locale === 'en' ? 'public/blog' : 'public/es/blog';
  return fs.readdirSync(root,{withFileTypes:true}).filter(e=>e.isDirectory()).map(e=>{
    const file=path.join(root,e.name,'index.html'); if(!fs.existsSync(file)) return null;
    const html=fs.readFileSync(file,'utf8');
    return {url:`/${locale==='en'?'':'es/'}blog/${e.name}/`,title:html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i)?.[1].replace(/<[^>]+>/g,'').trim(),html};
  }).filter(a=>a?.title);
};
const style=`<style>*{box-sizing:border-box}body{margin:0;background:#13151a;color:#e7e9ee;font:16px/1.6 system-ui,sans-serif}a{color:#e8a33d}.wrap{max-width:1100px;margin:auto;padding:32px 24px 80px}nav{display:flex;justify-content:space-between;gap:20px;align-items:center;padding:18px 0;border-bottom:1px solid #2e343f}nav a{text-decoration:none}.hero{padding:72px 0 48px;max-width:800px}.eyebrow{color:#e8a33d;text-transform:uppercase;font-size:12px;font-weight:800;letter-spacing:.12em}h1{font-size:clamp(38px,7vw,72px);line-height:1.05;margin:16px 0}h2{font-size:30px;margin-top:56px}.lead{font-size:20px;color:#aeb5c2}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:16px}.card{display:block;border:1px solid #2e343f;background:#1c1f26;border-radius:14px;padding:22px;text-decoration:none;color:#e7e9ee}.card:hover{border-color:#e8a33d}.card strong{display:block;font-size:18px;margin-bottom:8px}.card p{color:#aeb5c2;margin:0}.cta{display:inline-block;background:#e8a33d;color:#13151a!important;padding:12px 18px;border-radius:9px;text-decoration:none;font-weight:800;margin:12px 10px 0 0}.secondary{background:transparent;color:#e8a33d!important;border:1px solid #e8a33d}.note{border-left:3px solid #e8a33d;padding:10px 18px;color:#aeb5c2;margin:28px 0}footer{border-top:1px solid #2e343f;margin-top:64px;padding-top:28px;color:#8b93a3}@media(max-width:600px){nav{align-items:flex-start;flex-direction:column}.hero{padding-top:48px}}</style>`;
function shell({locale,title,description,canonical,body,schema}){const es=locale==='es';const alternate=es?canonical.replace('/es/recursos/','/resources/').replace('/es/ecosistema-forge/','/forge-ecosystem/'):canonical.replace('/resources/','/es/recursos/').replace('/forge-ecosystem/','/es/ecosistema-forge/');const isEco=/\/(?:es\/)?(?:ecosistema-forge|forge-ecosystem)\/$/.test(canonical),isHub=/\/(?:es\/recursos|resources)\/$/.test(canonical),de=isEco?'https://wendygostudio.com/de/forge-oekosystem/':isHub?'https://wendygostudio.com/de/ressourcen/':'',fr=isEco?'https://wendygostudio.com/fr/ecosysteme-forge/':isHub?'https://wendygostudio.com/fr/ressources/':'';return `<!doctype html><html lang="${locale}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} | Wendygo Studio</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${canonical}"><link rel="alternate" hreflang="${locale}" href="${canonical}"><link rel="alternate" hreflang="${es?'en':'es'}" href="${alternate}">${de?`<link rel="alternate" hreflang="de" href="${de}"><link rel="alternate" hreflang="fr" href="${fr}">`:''}<link rel="alternate" hreflang="x-default" href="${es?alternate:canonical}">${schema?`<script type="application/ld+json">${JSON.stringify(schema)}</script>`:''}${style}</head><body><div class="wrap"><nav><a href="/${es?'es/':''}"><strong>Wendygo Studio</strong></a><div><a href="/${es?'es/recursos/':'resources/'}">${es?'Recursos':'Resources'}</a> · <a href="/${es?'es/ecosistema-forge/':'forge-ecosystem/'}">${es?'Ecosistema':'Ecosystem'}</a> · <a href="/${es?'es/blog/':'blog/'}">Blog</a></div></nav>${body}<footer>© 2026 Wendygo Studio · ${es?'Herramientas locales y respetuosas con la privacidad.':'Local, privacy-first browser tools.'}</footer></div><script defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token":"e5e5861ded154e779510bf3f84cdd7fd"}'></script></body></html>`}
const writes=[];
for(const locale of ['en','es']){
  const list=articles(locale), es=locale==='es';
  for(const [slug,hub] of Object.entries(hubs)){
    const product=products[hub.product], copy=hub[locale];
    const matches=list.filter(a=>a.html.toLowerCase().includes(product.name.toLowerCase())).slice(0,12);
    const useCase=hub.useCase?.[locale];
    const body=`<main><section class="hero"><div class="eyebrow">${es?'Centro temático':'Topic hub'}</div><h1>${copy[0]}</h1><p class="lead">${copy[1]}</p><a class="cta" href="/${es?'es/':''}${hub.product}/">${es?'Conocer':'Explore'} ${product.name}</a><a class="cta secondary" href="/${es?'es/recursos/':'resources/'}">${es?'Todos los recursos':'All resources'}</a></section><section><h2>${es?'Guías seleccionadas':'Selected guides'}</h2><div class="grid">${useCase?`<a class="card" href="${useCase[0]}"><strong>${esc(useCase[1])}</strong><p>${es?'Descripción completa de funciones, privacidad y formatos compatibles.':'Full overview of features, privacy and supported formats.'}</p></a>`:''}${matches.map(a=>`<a class="card" href="${a.url}"><strong>${esc(a.title)}</strong><p>${es?'Guía relacionada con':'A practical guide connected to'} ${product.name}.</p></a>`).join('')||`<div class="card"><strong>${es?'Próximamente':'Coming next'}</strong><p>${es?'Estamos organizando las guías de esta categoría.':'We are organizing guides for this category.'}</p></div>`}</div></section><aside class="note">${esc(product.summary[locale])}</aside></main>`;
    const base=es?`public/es/recursos/${slug}`:`public/resources/${slug}`;
    writes.push([`${base}/index.html`,shell({locale,title:copy[0],description:copy[1],canonical:`https://wendygostudio.com/${es?'es/recursos':'resources'}/${slug}/`,body,schema:{'@context':'https://schema.org','@type':'CollectionPage',name:copy[0],about:product.name}})]);
  }
  const cards=Object.entries(hubs).map(([slug,h])=>`<a class="card" href="/${es?'es/recursos':'resources'}/${slug}/"><strong>${esc(h[locale][0])}</strong><p>${esc(h[locale][1])}</p></a>`).join('');
  writes.push([`${es?'public/es/recursos':'public/resources'}/index.html`,shell({locale,title:es?'Recursos por tema':'Resources by topic',description:es?'Explora las guías de Wendygo por necesidad y herramienta.':'Explore Wendygo guides by task and tool.',canonical:`https://wendygostudio.com/${es?'es/recursos':'resources'}/`,body:`<main><section class="hero"><div class="eyebrow">Wendygo Studio</div><h1>${es?'Recursos por tema':'Resources by topic'}</h1><p class="lead">${es?'Encuentra la herramienta y las guías adecuadas según lo que necesitas hacer.':'Find the right tool and practical guides for the job you need to do.'}</p></section><div class="grid">${cards}</div></main>`,schema:{'@context':'https://schema.org','@type':'CollectionPage',name:es?'Recursos de Wendygo Studio':'Wendygo Studio resources'}})]);
  const productCards=Object.entries(products).map(([id,p])=>`<a class="card" href="/${es?'es/':''}${id}/"><strong>${p.name}</strong><p>${esc(p.summary[locale])}</p></a>`).join('');
  writes.push([`${es?'public/es/ecosistema-forge':'public/forge-ecosystem'}/index.html`,shell({locale,title:es?'El ecosistema Forge':'The Forge ecosystem',description:es?'Seis extensiones, una filosofía local y respetuosa con la privacidad.':'Six extensions, one local and privacy-first philosophy.',canonical:`https://wendygostudio.com/${es?'es/ecosistema-forge':'forge-ecosystem'}/`,body:`<main><section class="hero"><div class="eyebrow">Wendygo Studio</div><h1>${es?'Un ecosistema, seis herramientas especializadas':'One ecosystem, six focused tools'}</h1><p class="lead">${es?'Cada Forge resuelve una tarea concreta. Todas comparten procesamiento local como base, precios claros y control del usuario.':'Each Forge solves a focused task. They share local-first processing, clear pricing and user control.'}</p><a class="cta" href="/${es?'es/recursos/':'resources/'}">${es?'Explorar por necesidad':'Explore by need'}</a></section><h2>${es?'La familia Forge':'The Forge family'}</h2><div class="grid">${productCards}</div><aside class="note">${es?'El procesamiento principal ocurre localmente. Los datos solo salen del navegador cuando eliges explícitamente un proveedor externo con tu propia clave.':'Core processing runs locally. Data only leaves the browser when you explicitly choose an external provider with your own key.'}</aside></main>`,schema:{'@context':'https://schema.org','@type':'ItemList',name:es?'Ecosistema Forge':'Forge ecosystem',itemListElement:Object.entries(products).map(([id,p],i)=>({'@type':'ListItem',position:i+1,name:p.name,url:`https://wendygostudio.com/${es?'es/':''}${id}/`}))}})]);
}
let changed=0;for(const [file,html] of writes){const current=fs.existsSync(file)?fs.readFileSync(file,'utf8'):'';if(current!==html){changed++;if(!check){fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,html)}}}
console.log(`${check?'Checked':'Rendered'} ${writes.length} resource and ecosystem pages; ${changed} ${check?'out of sync':'updated'}`);if(check&&changed)process.exit(1);
