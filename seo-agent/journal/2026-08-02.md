# Daily SEO — 2026-08-02

## Persistencia de tareas

- `translationDebt: OPEN`: el siguiente Daily SEO debe recalcular los grupos
  de `translationKey` sin EN, ES, DE, FR, IT y PT y traducir una tanda de 2–4
  grupos prioritarios antes de cerrar la ejecución.
- `socialDebt: OPEN`: Bluesky y Dev.to deben comprobarse en cada ejecución.
  Existe material pendiente en `seo-agent/pending-publish/`; si no hay salida
  de red, se conserva la cola y se documenta el bloqueo, sin simular actividad.

Se reforzaron `prompts/daily-seo.md` y `CODEX-RUNBOOK.md` para que ninguna
ejecución vuelva a darse por completa mientras estas dos colas sigan abiertas.

## Segundo ciclo

- Se ejecutó `npm.cmd run seo:fix` y se cerró la puerta de calidad: 518 HTML,
  0 errores y 7 tests correctos.
- La deuda estructurada recalculada queda en 62 grupos incompletos. La primera
  tanda automática de traducción se interrumpió por el proveedor externo y no
  se conservaron archivos parciales para evitar contenido mojibake. Por tanto,
  `translationDebt: OPEN` continúa vigente para la siguiente ejecución.
- Bluesky sí tuvo actividad real: 2 likes y 2 follows relevantes en
  productividad/foco. Dev.to respondió HTTP 200 al comprobar la cuenta; no se
  publicó porque no había un artículo nuevo o una actualización editorial
  sustancial en este ciclo. `socialDebt: OPEN` conserva los borradores pendientes.

## Tercer ciclo

- Se volvió a ejecutar el mantenimiento SEO completo; validación 518/0 y tests
  7/7 correctos.
- La deuda sigue en 62 grupos. No se forzó otra tanda de traducción automática
  tras el bloqueo del proveedor y la detección de archivos parciales; continúa
  abierta para la próxima ejecución.
- Se respetó el límite de interacción de Bluesky del día tras los 2 likes y 2
  follows ya realizados. Dev.to volvió a responder HTTP 200 y se comprobó que
  no había motivo editorial para publicar otra adaptación.

## Corrección del motor de traducción

- `seo:fix` ya invoca `scripts/translate-daily-articles.mjs --auto` y se añadió
  `translate:batch` para repetir la tanda de forma explícita. El proceso limita
  concurrencia, aplica timeout, valida respuestas del proveedor y solo escribe
  un grupo cuando todas sus salidas están completas.
- Se completaron 10 grupos (50 archivos de idioma) en tres tandas. La auditoría
  actual muestra 91 grupos, 41 completos y 50 incompletos; por eso
  `translationDebt: OPEN` sigue siendo obligatorio.
- Se repararon 58 metadatos corruptos (`title`/`description` `>-`) provocados por
  respuestas incompletas del proveedor. No quedan metadatos con ese marcador.
- `migrate-legacy-blog.mjs` ignora ahora fuentes estructuradas `schemaVersion: 1`,
  evitando que el parser antiguo deshaga las traducciones en cada ciclo.
- La validación de contenido pasa; quedan discrepancias heredadas de generación
  de hreflang/selector en la cadena de scripts que deben estabilizarse en el
  siguiente mantenimiento. No se cierra la deuda hasta que la cobertura sea
  6/6 por grupo.
