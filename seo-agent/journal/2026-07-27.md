# Daily SEO — 2026-07-27

Primera ejecución bajo el nuevo prompt "Growth Engineer" (`seo-agent/prompts/daily-seo.md` reescrito ayer). Mentalidad: la acción de mayor impacto hoy, no "toca artículo".

## Paso 0 — Memoria y contexto

- Leído `seo-agent/MEMORY.md` completo. Sin experimentos pendientes de medir (es la primera ejecución desde que existe el archivo).
- No hay journal de 2026-07-26 en `seo-agent/journal/` — el trabajo de ayer (traducción de todas las herramientas a 6 idiomas, fix de asociaciones de producto en los hubs de recursos, varios artículos multilingües) se hizo directamente por Damián en su máquina, fuera del pipeline `orchestrator.sh`, así que no generó journal. Lo dejo anotado para no asumir continuidad de contexto que no existe.

## Corrección crítica encontrada antes de poder analizar Search Console

`analytics-data.json` no era UTF-8 válido — 5 bytes `\xf1` sueltos (la "ñ" de "tamaño" en varias queries de Instagram) escritos en Latin-1 en vez de UTF-8, probablemente por `analytics-fetch.py` en alguna ejecución anterior. Cualquier script que intente `JSON.parse`/`json.load` estricto sobre ese archivo fallaría. Corregido reemplazando los bytes sueltos por su codificación UTF-8 correcta (`ñ` → `0xc3 0xb1`); verificado que el resto del archivo ya era UTF-8 válido (solo esos 5 bytes estaban mal) y que el JSON parsea y el contenido tiene sentido (`tamaño` en vez de basura).

Intenté refrescar los datos con `python3 seo-agent/scripts/analytics-fetch.py` para tener algo más reciente que 2026-07-25. Falló: faltan las dependencias de Google (`google-auth` etc.) en este entorno, y además el script escribe la salida en una ruta Windows literal (`C:/Users/.../analytics-data.json`) que no existe dentro de este sandbox Linux — este fetch está pensado para correr en tu máquina real vía `orchestrator.sh`, no desde aquí. Seguí con los datos existentes (2026-07-25, ya corregidos), no simulé un fetch nuevo.

## Paso 1 — Search Console Intelligence

Con los 50 queries / 50 páginas del muestreo de `analytics-data.json` (periodo 2026-06-24 a 2026-07-22):

- **Páginas en posición 8-20** (candidatas a Top 10): `eu-warranty-rights-explained` (pos 16.1, 40 impresiones, 0 clics), `resize-image-for-pinterest-pin` (pos 17.0, 36 impr., 1 clic), `cyberchef-alternatives` (pos 14.5, 34 impr., 1 clic), y varias más con menos impresiones.
- Antes de tocar nada, comprobé cuántos enlaces internos entrantes tiene ya cada una (`grep -rl` sobre `public/blog/*/index.html`):
  - `eu-warranty-rights-explained`: **9 enlaces entrantes ya existentes**. No es un problema de enlazado — tocar más aquí habría sido relleno sin base.
  - `cyberchef-alternatives`: **3 enlaces entrantes ya existentes** (clean-text-online, decode-jwt-online, forest-app-alternatives-chrome). Razonablemente cubierto.
  - `resize-image-for-pinterest-pin`: **0 enlaces entrantes**. Hueco real y verificado con datos, no una suposición.

## Paso 2 — Enlazado interno dirigido (acción de hoy)

Añadí 6 enlaces internos hacia la guía de Pinterest, repartidos por afinidad temática real (todas son guías de FrameForge sobre redimensionar/recortar imágenes para redes sociales), verificando cada `href` contra el directorio antes de escribirlo:

- EN: `batch-resize-images-chrome-extension`, `crop-images-social-media-chrome`, `free-thumbnail-tool-no-login` → todas ahora enlazan a `/blog/resize-image-for-pinterest-pin/`.
- ES: `redimensionar-imagenes-lote-extension-chrome`, `recortar-imagenes-redes-sociales-chrome`, `herramienta-miniaturas-gratis-sin-registro` → todas ahora enlazan a `/es/blog/redimensionar-imagen-pinterest/`.

No toqué `eu-warranty-rights-explained` ni `cyberchef-alternatives` — ya tienen enlazado interno saludable; forzar más ahí no tenía justificación de datos.

Registrado como experimento `id: 1` en `MEMORY.md` (hipótesis + fecha de medición a 30 días).

## Paso 3 — Mantenimiento de contenido

Revisé `eu-warranty-rights-explained` (candidato a CTR bajo: 40 impresiones, 0 clics) por si el `<title>`/`meta description` tenían un problema obvio de CTR. Título y descripción ya son claros, incluyen el número concreto (2 años) y responden a la intención de búsqueda — no encontré una hipótesis de mejora con suficiente convicción como para reescribirlo hoy sin más señal (podría ser competencia en la SERP, no el snippet). Lo dejo anotado para revisar de nuevo si sigue en 0 clics dentro de unas semanas, en vez de cambiarlo sin motivo claro.

## Ruta B — contenido nuevo

No se publicó ningún artículo nuevo hoy. Según la jerarquía de prioridades del nuevo prompt, el enlazado interno dirigido (con hueco real y verificado) tenía más retorno esperado que añadir un artículo más — y la restricción explícita es no publicar solo porque "toca".

## Validación

- `node scripts/validate-site.mjs`: 430 archivos HTML, 0 errores (enlaces nuevos verificados, ninguno roto).
- `node scripts/validate-governance.mjs`: 0 errores (18s, tras el fix de rendimiento de ayer).
- `node scripts/validate-content-hygiene.mjs`: 0 errores.

## Plan para mañana / próxima ejecución

- Medir el experimento `id: 1` el 2026-08-26 (30 días) usando datos frescos de Search Console.
- Si para entonces se puede correr `analytics-fetch.py` con dependencias instaladas y desde la máquina real (no este sandbox), hacerlo — los datos actuales tienen ya 2 días y solo cubren un muestreo de 50 queries/50 páginas, no el export completo.
- Revisar `eu-warranty-rights-explained` de nuevo si sigue en 0 clics con datos más recientes, esta vez con hipótesis concreta de qué cambiar (no solo "reescribir por reescribir").
